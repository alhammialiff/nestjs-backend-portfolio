import { Injectable } from '@nestjs/common';

import type { LoginDto } from './../model/loginDto.model';

@Injectable()
export class AuthService {

    /**
    * Placeholder as I decide if it is necessary to further breakdown 
    * Auth Middleware into smaller AuthService methods
    */
   
}
